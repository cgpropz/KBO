# Offline ML tooling (ml/ only; never installed by the live pipeline or Vercel).
# Phase 1 scripts (datasets, replay, baseline) are Python-stdlib only and need
# none of these. They are pinned here for Phase 2 (calibrators / tuning).
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2
scipy>=1.11,<2
